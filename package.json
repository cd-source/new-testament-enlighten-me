{
  "name": "enlighten-me",
  "version": "0.1.0",
  "private": true,
  "description": "Enlighten-Me: free local KJV scripture with paid AI imagery.",
  "scripts": {
    "build": "node scripts/build-capacitor-web.js",
    "validate": "npm run build && node --check script.js && node scripts/validate-data.js",
    "cap:sync": "npm run build && npx cap sync ios",
    "cap:open": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^7.6.2",
    "@capacitor/filesystem": "^7.1.8",
    "@capacitor/ios": "^7.6.2",
    "@capacitor/share": "^7.0.4",
    "@supabase/supabase-js": "^2.45.0",
    "@vercel/analytics": "^2.0.1",
    "jsonwebtoken": "^9.0.2",
    "stripe": "^17.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.6.2"
  }
}
